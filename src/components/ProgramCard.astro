---
interface Props {
	name: string;
	description: string;
	image: string;
	link?: string;
}

const { name, description, image, link } = Astro.props;
---

<div class="card text-center h-full flex flex-col p-6" data-reveal>
	<div class="w-full h-auto mb-4 flex items-center justify-center">
		<img src={image} alt={name} class="h-full w-full object-cover rounded-lg" loading="lazy" />
	</div>

	<h2 class="text-primary uppercase tracking-wide">{name}</h2>
	<div class="underline-accent mx-auto bg-tint"></div>
	<p class="text-sm text-text/80 mb-6 flex-grow">{description}</p>

	{link && (
		<a href={link} class="btn-secondary w-full">
			Viac o službe
		</a>
	)}
</div>
