---
const { icon, label } = Astro.props;
---

<div class="bg-white/10 rounded-lg p-4 text-center flex flex-col space-y-4">
    <img src={icon} alt="illustration" class="w-full">
    <h4 class="text-bg/90 font-semibold">{label}</h4>
</div>